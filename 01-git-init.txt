
﻿git init : Assign the folder is used to store data.

คำสั่ง git init ทำให้ folder เป็น master *ใช้ครั้งเดียว
﻿
git init คือ แสดงความต้องการที่จะใช้ Folder นี้ใช้ได้ครั้งเดียวต่อ Folder

git init : Use just only once
